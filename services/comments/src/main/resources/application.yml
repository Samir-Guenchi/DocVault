server:
  port: 8082

spring:
  application:
    name: comments-service
  datasource:
    url: jdbc:h2:mem:commentsdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
      path: /h2-console
